# URL Shortener in Flask

A minimalistic url shortener made using Flask, a micro-framework for Python.

## Prerequisites

You should have the following installed:
- Python 3.11.5
- Flask, Jinja2, MarkupSafe, Werkzeug, blinker, click, itsdangerous, pip, setuptools, wheel